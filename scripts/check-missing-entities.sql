-- Check which employees and vehicles are missing from the database
-- List all employees currently in database
SELECT 'EMPLOYEES' as type, name FROM employees ORDER BY name;

-- List all vehicles currently in database  
SELECT 'VEHICLES' as type, name FROM vehicles ORDER BY name;
