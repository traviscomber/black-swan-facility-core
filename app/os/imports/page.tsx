import { OsWorkspace } from '@/components/os-workspace'

export default function Page() {
  return <OsWorkspace workspace="imports" title="Canonical Imports" description="Review-first employee and inventory source batches. Santi's source files remain required before any entity allocation is applied." />
}
