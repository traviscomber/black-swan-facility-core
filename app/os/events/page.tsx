import { OsWorkspace } from '@/components/os-workspace'

export default function Page() {
  return <OsWorkspace workspace="events" title="Events" description="Member-linked event planning, external service providers, operational status and Education output." />
}
