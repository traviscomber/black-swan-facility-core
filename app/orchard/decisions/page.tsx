"use client"

import Link from "next/link"
import type { ReactNode } from "react"
import { useCallback, useEffect, useMemo, useState } from "react"
import { AlertTriangle, CheckCircle2, Clock3, RefreshCw, ShieldAlert, Sprout } from "lucide-react"
import { AppLayout } from "@/components/app-layout"
import { OrchardNavigation } from "@/components/orchard/orchard-navigation"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { createBrowserClient } from "@/lib/supabase/client"
import { useLanguage } from "@/lib/hooks/use-language"

type Lifecycle={crop_succession_id:string;crop_cycle_id:string;sequence_no:number;effective_status:string;planned_sow_date:string;planned_transplant_date:string|null;planned_first_harvest_date:string|null;first_planting_date:string|null;harvest_passes:number}
type Succession={id:string;crop_cycle_id:string;planned_plants:number|null;germination_rate_pct:number|null;seeds_per_plant:number|null;status:string}
type Cycle={id:string;crop_name:string;variety:string|null}
type SeedLot={crop_name:string;variety:string|null;quantity_seeds:number;expiry_date:string|null}
type Allocation={id:string;bed_id:string;crop_succession_id:string;planned_start_date:string;planned_end_date:string}
type Bed={id:string;plot_id:string;name:string}
type Plot={id:string;name:string}
type Pest={id:string;crop_id:string;pest_type:string|null;disease_name:string|null;severity_level:string|null;affected_percentage:number|null}
type Crop={id:string;crop_name:string;variety:string|null;estimated_yield:number|null;actual_yield:number|null;yield_unit:string|null}
type Task={id:string;title:string;priority:string|null;status:string;due_date:string|null}
type Decision={id:string;severity:"critical"|"attention"|"ready"|"watch";category:string;title:string;detail:string;rule:string;href:string}

const image=(id:string,w=1800)=>`https://images.unsplash.com/photo-${id}?auto=format&fit=crop&q=92&w=${w}`
const HERO={src:image("1457530378978-8bac673b8062",2200),alt:"Field rows viewed from above"}
const CATEGORY_PHOTOS:Record<string,{src:string;alt:string}>={Lifecycle:{src:image("1416879595882-3373a0480b5b"),alt:"Growing plants"},Harvest:{src:image("1471194402529-8e0f5a675de6"),alt:"Harvest ready vegetables"},Seeds:{src:image("1445282768818-728615cc910a"),alt:"Seeds and planting"},Rotation:{src:image("1523741543316-beb7fc7023d8"),alt:"Crop rows"},Health:{src:image("1464226184884-fa280b87c399"),alt:"Plant inspection"},Work:{src:image("1500937386664-56d1dfef3854"),alt:"Field work"},Performance:{src:image("1498579397066-22750a3cb424"),alt:"Productive crop"}}
const FALLBACK={src:image("1488459716781-31db52582fe9"),alt:"Orchard field"}
const normalize=(v:string|null|undefined)=>(v??"").trim().toLowerCase()
const cropKey=(crop:string,variety:string|null)=>`${normalize(crop)}::${normalize(variety)}`
const labelCrop=(crop:string,variety:string|null)=>`${crop}${variety?` · ${variety}`:""}`
const localToday=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
const daysBetween=(a:string,b:string)=>Math.round((new Date(`${a}T12:00:00`).getTime()-new Date(`${b}T12:00:00`).getTime())/86400000)
const seedsNeeded=(s:Succession)=>s.planned_plants&&s.germination_rate_pct&&s.seeds_per_plant?Math.ceil(s.planned_plants*s.seeds_per_plant/(s.germination_rate_pct/100)):0

export default function OrchardDecisionEnginePage(){
 const supabase=useMemo(()=>createBrowserClient(),[]);const {language}=useLanguage();const es=language==="es"
 const [loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[lifecycle,setLifecycle]=useState<Lifecycle[]>([]),[successions,setSuccessions]=useState<Succession[]>([]),[cycles,setCycles]=useState<Cycle[]>([]),[seedLots,setSeedLots]=useState<SeedLot[]>([]),[allocations,setAllocations]=useState<Allocation[]>([]),[beds,setBeds]=useState<Bed[]>([]),[plots,setPlots]=useState<Plot[]>([]),[pests,setPests]=useState<Pest[]>([]),[crops,setCrops]=useState<Crop[]>([]),[tasks,setTasks]=useState<Task[]>([])
 const load=useCallback(async()=>{setLoading(true);setError(null);const [lc,s,cy,seed,a,b,p,pe,c,t]=await Promise.all([supabase.from("orchard_succession_lifecycle").select("crop_succession_id,crop_cycle_id,sequence_no,effective_status,planned_sow_date,planned_transplant_date,planned_first_harvest_date,first_planting_date,harvest_passes"),supabase.from("orchard_crop_successions").select("id,crop_cycle_id,planned_plants,germination_rate_pct,seeds_per_plant,status"),supabase.from("orchard_crop_cycles").select("id,crop_name,variety"),supabase.from("orchard_seed_lots").select("crop_name,variety,quantity_seeds,expiry_date"),supabase.from("orchard_bed_allocations").select("id,bed_id,crop_succession_id,planned_start_date,planned_end_date"),supabase.from("orchard_beds").select("id,plot_id,name"),supabase.from("orchard_plots").select("id,name"),supabase.from("orchard_pest_logs").select("id,crop_id,pest_type,disease_name,severity_level,affected_percentage"),supabase.from("orchard_crops").select("id,crop_name,variety,estimated_yield,actual_yield,yield_unit"),supabase.from("tasks").select("id,title,priority,status,due_date").eq("operational_area","huerto_vinedo")]);const e=lc.error??s.error??cy.error??seed.error??a.error??b.error??p.error??pe.error??c.error??t.error;if(e)setError(e.message);else{setLifecycle((lc.data??[]) as Lifecycle[]);setSuccessions((s.data??[]) as Succession[]);setCycles((cy.data??[]) as Cycle[]);setSeedLots((seed.data??[]) as SeedLot[]);setAllocations((a.data??[]) as Allocation[]);setBeds((b.data??[]) as Bed[]);setPlots((p.data??[]) as Plot[]);setPests((pe.data??[]) as Pest[]);setCrops((c.data??[]) as Crop[]);setTasks((t.data??[]) as Task[])}setLoading(false)},[supabase]);useEffect(()=>{void load()},[load])
 const decisions=useMemo<Decision[]>(()=>{const today=localToday();const cycleById=new Map(cycles.map(x=>[x.id,x])),successionById=new Map(successions.map(x=>[x.id,x])),bedById=new Map(beds.map(x=>[x.id,x])),plotById=new Map(plots.map(x=>[x.id,x])),cropById=new Map(crops.map(x=>[x.id,x]));const result:Decision[]=[];const successionLabel=(id:string)=>{const s=successionById.get(id),c=s?cycleById.get(s.crop_cycle_id):null;return s&&c?`${labelCrop(c.crop_name,c.variety)} #${lifecycle.find(x=>x.crop_succession_id===id)?.sequence_no??""}`:id}
 lifecycle.forEach(x=>{const label=successionLabel(x.crop_succession_id);if(x.effective_status==="planned"&&x.planned_sow_date<today)result.push({id:`late-sow-${x.crop_succession_id}`,severity:"critical",category:"Lifecycle",title:`${label}: sowing overdue`,detail:`${Math.abs(daysBetween(x.planned_sow_date,today))} day(s) past the recorded planned sow date.`,rule:"Planned sow date is before today and lifecycle is still planned.",href:"/orchard/lifecycle"});if(["nursery","hardening","sown"].includes(x.effective_status)&&x.planned_transplant_date&&x.planned_transplant_date<today&&!x.first_planting_date)result.push({id:`late-transplant-${x.crop_succession_id}`,severity:"attention",category:"Lifecycle",title:`${label}: transplant overdue`,detail:`${Math.abs(daysBetween(x.planned_transplant_date,today))} day(s) past the recorded planned transplant date.`,rule:"Planned transplant date is before today and no field planting is recorded.",href:"/orchard/nursery"});if(x.effective_status==="harvest_ready")result.push({id:`ready-${x.crop_succession_id}`,severity:"ready",category:"Harvest",title:`${label}: harvest ready`,detail:x.planned_first_harvest_date?`Recorded planned first harvest: ${x.planned_first_harvest_date}.`:"Lifecycle currently reports harvest_ready.",rule:"Effective lifecycle status is harvest_ready.",href:"/orchard/harvest"})})
 const required=new Map<string,{crop:string;variety:string|null;needed:number}>();successions.filter(x=>!["completed","cancelled","harvesting"].includes(x.status)).forEach(x=>{const c=cycleById.get(x.crop_cycle_id),needed=seedsNeeded(x);if(!c||!needed)return;const key=cropKey(c.crop_name,c.variety),cur=required.get(key);required.set(key,{crop:c.crop_name,variety:c.variety,needed:(cur?.needed??0)+needed})});const stock=new Map<string,number>();seedLots.forEach(l=>{if(l.expiry_date&&l.expiry_date<today)return;const key=cropKey(l.crop_name,l.variety);stock.set(key,(stock.get(key)??0)+Math.max(0,l.quantity_seeds??0))});required.forEach((req,key)=>{const available=stock.get(key)??0;if(available<req.needed)result.push({id:`seed-${key}`,severity:available===0?"critical":"attention",category:"Seeds",title:`${labelCrop(req.crop,req.variety)}: seed shortage`,detail:`${available.toLocaleString()} available vs ${req.needed.toLocaleString()} calculated requirement.`,rule:"Non-expired recorded seed stock is below the calculated requirement from planned plants, germination rate and seeds per plant.",href:"/orchard/nursery"})})
 const byBed=new Map<string,Allocation[]>();allocations.forEach(x=>byBed.set(x.bed_id,[...(byBed.get(x.bed_id)??[]),x]));byBed.forEach((items,bedId)=>{const ordered=[...items].sort((a,b)=>a.planned_start_date.localeCompare(b.planned_start_date)),bed=bedById.get(bedId),plot=bed?plotById.get(bed.plot_id):null,bedLabel=bed?`${plot?.name?`${plot.name} · `:""}${bed.name}`:bedId;ordered.forEach((x,i)=>{const next=ordered[i+1];if(!next)return;const cs=successionById.get(x.crop_succession_id),ns=successionById.get(next.crop_succession_id),cc=cs?cycleById.get(cs.crop_cycle_id):null,nc=ns?cycleById.get(ns.crop_cycle_id):null;if(cc&&nc&&normalize(cc.crop_name)===normalize(nc.crop_name))result.push({id:`rotation-${x.id}-${next.id}`,severity:"watch",category:"Rotation",title:`${bedLabel}: repeated crop sequence`,detail:`${labelCrop(cc.crop_name,cc.variety)} is followed by ${labelCrop(nc.crop_name,nc.variety)}.`,rule:"Two consecutive recorded allocations on the same bed use the same crop name. This is only a rotation review flag, not a disease-risk diagnosis.",href:"/orchard/crop-map"})})})
 pests.forEach(x=>{if(!["high","critical"].includes(normalize(x.severity_level)))return;const crop=cropById.get(x.crop_id);result.push({id:`health-${x.id}`,severity:normalize(x.severity_level)==="critical"?"critical":"attention",category:"Health",title:`${crop?labelCrop(crop.crop_name,crop.variety):"Crop"}: high-severity observation`,detail:`${x.pest_type||x.disease_name||"Health observation"}${x.affected_percentage!=null?` · ${x.affected_percentage}% affected`:""}.`,rule:"The recorded observation severity is high or critical. This does not infer whether the issue remains open or resolved.",href:"/orchard/pests"})})
 tasks.filter(x=>!["done","completed","cancelled","completada","cancelada"].includes(normalize(x.status))&&x.due_date&&x.due_date<today).forEach(x=>result.push({id:`task-${x.id}`,severity:["urgent","critical","high","urgente","alta"].includes(normalize(x.priority))?"critical":"attention",category:"Work",title:`Overdue: ${x.title}`,detail:`Recorded due date: ${x.due_date}.`,rule:"Open Orchard task has a due date before today.",href:"/orchard/work"}))
 crops.forEach(c=>{if(c.estimated_yield==null||c.actual_yield==null||c.estimated_yield<=0||!c.yield_unit||c.actual_yield>=c.estimated_yield)return;const pct=Math.round(c.actual_yield/c.estimated_yield*100);result.push({id:`yield-${c.id}`,severity:"watch",category:"Performance",title:`${labelCrop(c.crop_name,c.variety)}: actual yield below estimate`,detail:`${c.actual_yield} / ${c.estimated_yield} ${c.yield_unit} (${pct}%).`,rule:"Recorded actual yield is below the recorded estimate in the same unit. No severity threshold is applied.",href:"/orchard/harvest"})})
 const rank={critical:0,attention:1,ready:2,watch:3};return result.sort((a,b)=>rank[a.severity]-rank[b.severity]||a.category.localeCompare(b.category)||a.title.localeCompare(b.title))},[allocations,beds,crops,cycles,lifecycle,pests,plots,seedLots,successions,tasks])
 const counts=decisions.reduce<Record<Decision["severity"],number>>((a,x)=>({...a,[x.severity]:a[x.severity]+1}),{critical:0,attention:0,ready:0,watch:0});const rules=["Overdue sowing or transplant from recorded lifecycle dates.","Lifecycle explicitly marked harvest_ready.","Non-expired seed stock below a requirement calculated from recorded planning inputs.","Repeated crop names on consecutive bed allocations, shown only as a rotation review flag.","High/critical health observations, without inferring unresolved treatment status.","Open tasks whose recorded due date is already past.","Actual yield below recorded estimate, shown as a variance without an arbitrary severity threshold."]
 const text=es?{title:"Cockpit de Decisiones",description:"Excepciones operativas derivadas de hechos registrados. Se eliminaron ventanas y umbrales no documentados.",total:"Señales",critical:"Crítico",attention:"Atención",ready:"Listo",watch:"Revisar",rules:"Reglas activas",rulesHelp:"Cada regla usa estados, fechas o comparaciones explícitas del registro; no aplica umbrales agronómicos inventados.",open:"Abrir",none:"No hay excepciones actuales basadas en registros."}:{title:"Decision Cockpit",description:"Operational exceptions derived from recorded facts. Undocumented windows and thresholds have been removed.",total:"Signals",critical:"Critical",attention:"Attention",ready:"Ready",watch:"Review",rules:"Rules in force",rulesHelp:"Each rule uses explicit recorded states, dates or comparisons; no invented agronomic threshold is applied.",open:"Open",none:"No current record-based exceptions."}
 return <AppLayout><OrchardNavigation/><main className="mx-auto w-full max-w-[1560px] space-y-10 px-4 pb-16 pt-4 sm:px-6 lg:px-8"><section className="relative isolate min-h-[360px] overflow-hidden bg-neutral-950 sm:min-h-[420px]"><img src={HERO.src} alt={HERO.alt} className="absolute inset-0 h-full w-full object-cover opacity-100 [filter:none]"/><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(0,0,0,.88)_0%,rgba(0,0,0,.44)_58%,rgba(0,0,0,.14)_100%)]"/><div className="relative flex min-h-[360px] max-w-3xl flex-col justify-end p-6 text-white sm:min-h-[420px] sm:p-10"><p className="text-xs uppercase tracking-[0.2em] text-white/60">Orchard · Decision Intelligence</p><h1 className="mt-3 text-4xl font-medium tracking-[-0.03em] sm:text-5xl">{text.title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-white/75">{text.description}</p><div className="mt-6"><Button onClick={()=>void load()} disabled={loading} className="bg-white text-black hover:bg-white/90"><RefreshCw className={`mr-2 h-4 w-4 ${loading?"animate-spin":""}`}/>Refresh</Button></div></div></section>{error&&<Card className="border-destructive/60"><CardContent className="p-4 text-sm text-destructive">{error}</CardContent></Card>}<section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><Metric label={text.total} value={decisions.length} icon={<ShieldAlert className="h-4 w-4"/>}/><Metric label={text.critical} value={counts.critical} icon={<AlertTriangle className="h-4 w-4"/>}/><Metric label={text.attention} value={counts.attention} icon={<Clock3 className="h-4 w-4"/>}/><Metric label={text.ready} value={counts.ready} icon={<CheckCircle2 className="h-4 w-4"/>}/><Metric label={text.watch} value={counts.watch} icon={<Sprout className="h-4 w-4"/>}/></section><section className="grid gap-6 xl:grid-cols-[1fr_360px]"><div>{loading?<p className="text-sm text-muted-foreground">Loading…</p>:decisions.length===0?<div className="border border-dashed p-6 text-sm text-muted-foreground">{text.none}</div>:<div className="grid gap-4 md:grid-cols-2">{decisions.map(item=>{const photo=CATEGORY_PHOTOS[item.category]??FALLBACK;return <article key={item.id} className="overflow-hidden border bg-background"><div className="relative h-36 overflow-hidden"><img src={photo.src} alt={photo.alt} className="h-full w-full object-cover opacity-100 [filter:none]"/><div className="absolute inset-0 bg-[linear-gradient(0deg,rgba(0,0,0,.76)_0%,rgba(0,0,0,.08)_82%)]"/><div className="absolute inset-x-4 bottom-3"><div className="flex gap-2"><Badge variant={item.severity==="critical"?"destructive":"secondary"}>{item.severity}</Badge><Badge className="bg-black/40 text-white">{item.category}</Badge></div><h3 className="mt-2 line-clamp-2 font-medium text-white">{item.title}</h3></div></div><div className="p-4"><p className="text-sm leading-6">{item.detail}</p><div className="mt-4 border-t pt-3"><p className="text-[10px] uppercase tracking-[0.13em] text-muted-foreground">Rule</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{item.rule}</p></div><Button asChild size="sm" className="mt-4 w-full" variant="outline"><Link href={`/${language}${item.href}`}>{text.open}</Link></Button></div></article>})}</div>}</div><Card className="h-fit"><CardHeader><CardTitle>{text.rules}</CardTitle><CardDescription>{text.rulesHelp}</CardDescription></CardHeader><CardContent><ol className="space-y-4 text-sm text-muted-foreground">{rules.map((r,i)=><li key={r} className="flex gap-3 border-b pb-4 last:border-0 last:pb-0"><span className="font-mono text-xs text-foreground">{String(i+1).padStart(2,"0")}</span><span>{r}</span></li>)}</ol></CardContent></Card></section></main></AppLayout>
}
function Metric({label,value,icon}:{label:string;value:number;icon:ReactNode}){return <Card><CardContent className="flex items-center justify-between p-4"><div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-semibold">{value}</p></div><div className="border p-2 text-muted-foreground">{icon}</div></CardContent></Card>}